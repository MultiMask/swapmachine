# Karma / Bitcoin exchange deamon

Install `yarn`:

`npm install yarn -g`

Install:

`yarn install`

Launch:

`yarn start`

This deamon watches incoming transactions on Bitcoin testnet and transfers Karma tokens. And vice versa.